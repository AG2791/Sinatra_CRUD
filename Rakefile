# Figure why this has to be listed in the order below

require_relative './config/environment'

#the reason this file is here instead of the environement is becuase its only needed for rake tasks
require "sinatra/activerecord/rake"

#remove later
desc "console" #description 
task :console do
    Pry.start
end